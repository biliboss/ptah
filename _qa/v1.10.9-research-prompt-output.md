# Research output — Faber tech-narration optimization

External LLM research 2026-06-04. Excerpted; actionable defaults applied v1.10.9+.

## Numeric defaults (Faber medium Pt-BR, VITS-15M, MCV-trained)

| Knob | Current default | Research tech-rec | Rationale |
|---|---|---|---|
| `length_scale` | 0.95 (v1.10.8 tech) | **1.05** | MCV fast read-speech; +5% recovers intelligibility |
| `noise_scale` | 0.667 | **0.35** | 0.3–0.4 avoids mid-sentence pitch drift |
| `noise_w` | 0.85 | **0.45** | Consistent phoneme durations on symbol strings |

Counter-arg flagged: stock 0.667 less robotic; A/B before locking.

## Acronym + unit glossary additions

```
HTTP→H T T P, HTTPS→H T T P S, SSH→S S H, TCP→T C P, UDP→U D P,
YAML→iêimel, CSV→C S V, XML→X M L, PDF→P D F, IDE→I D É,
CI/CD→C I C D, ORM→O R M, EOF→E O F, UUID→U U I D, NATS→nats

Units: fps→quadros por segundo, dB→decibéis, px→pixels, TB→terabytes,
bps→bits por segundo, Mbps→megabits por segundo, Gbps→gigabits por segundo

Brands: Docker→dóquer, Nginx→enginx, PostgreSQL→pós-ti-grês-quiu-el,
SQLite→es-quiu-lai-ti, SurrealDB→surreal D B, FastAPI→fast A P I,
Pydantic→paidântic, Zsh→zi shell, Homebrew→home-briu
```

## Text-normalization rules

- **CamelCase**: split at uppercase (`SwiftUI`→Swift U I, `getConditioningLatents`→get Conditioning Latents)
- **Versions**: `1.10.8`→"versão um ponto dez ponto oito"
- **Commit hashes**: 7-char truncate, hex letter-by-letter + cardinals (`bdd352e`→"commit be-dê-dê-três-cinco-dois-e")
- **URLs**: protocol expand, domain only
- **File paths**: read final component
- **IPs**: full dotted cardinals
- **Hex**: `0xFF`→"zero-x F F"
- **Ranges**: `1–5`→"um a cinco", `1..5`→"um até cinco"
- **Ordinals**: `1º`→"primeiro", `2ª`→"segunda"

## ONNX runtime knobs

- `SetGraphOptimizationLevel(ORT_ENABLE_BASIC)` — bit-exact for cache keying
- `intra_op_num_threads=1` on Apple Silicon — VITS not parallel
- Disable memory arena — avoid GC pauses

## miniaudio playback knobs

- `ma_resample_algorithm_linear` `lpfOrder=8` (default 4) — reduces sibilant aliasing
- `ma_dither_mode_triangle` on int16 conversion
- Feed PCM at -3 dBFS headroom

## Acoustic post-processing (ffmpeg priority order)

1. RNNoise (~2ms): `arnndn=m=cb.rnnn`
2. EQ: HPF 80Hz + body +2.5dB@280Hz + cut -1.5dB@3.5kHz + air +1.8dB@10kHz
3. De-esser: `deesser=i=0.08:m=0.5`
4. Compressor 2:1: `acompressor=threshold=-18dB:ratio=2:attack=20:release=200:makeup=2dB`

Skip room IR.

## SSML knobs not yet exposed

- `<phoneme alphabet="ipa" ph="...">` — Anthropic `ˌæn.θɹəˈpɪk`, Mistral `miˈstɾal`, Groq `ɡɾɔk`, Ollama `oˈlama`
- `<sub alias="...">` — display vs spoken split for code identifiers
- List cadence: `<break time="200ms"/>` between items + `<prosody pitch="+5%">` on labels
- Bullet lift: `-`/`•` line start → `<prosody pitch="+5%">` wrap
- `<lang>` mid-utterance NOT supported by piper1-gpl — use `<phoneme>`

## PCM cache key

```
SHA256(normalize(ssml_text) + "|" + length + "|" + noise + "|" + noise_w + "|" + voice + "|" + model_sha[:8])
```

Skip `speaker_id` for single-speaker. Store S16LE @ 22050Hz, filename = 16 hex chars.

## A/B 8-run Resolution IV 2⁴⁻¹

Factors: A=length(0.95/1.10), B=noise(0.30/0.55), C=noise_w(0.40/0.65), D=EQ(off/on). Generator D=ABC.

Score: `Intelligibility×0.4 + Naturalness×0.25 + Fatigue×0.2 + Pace×0.15`

## Other tricks

- Code blocks: prefix "código:" + 300ms + `length_scale=1.15`
- Operators: `→`→retorna, `:=`→recebe, `!=`→diferente de
- Breathing: 80ms pink noise -42 dBFS between every 2-3 sentences
- List-end drop: `<prosody pitch="-10%" rate="slow">` on last 3 words
