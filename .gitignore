zig-out/
.zig-cache/

node_modules/
dist/
.astro/

.DS_Store
*.log

# v0.6 libpiper FFI: cloned C source + build artifacts (~400MB).
# Reproduce via vendor/README.md. Voice models live in ~/.cache/agent-tts/voices/.
vendor/piper1-gpl/

# v0.7 zaudio + miniaudio vendored sources (~100k LoC single-header lib).
# Reproduce via vendor/README.md. Pinned commit in vendor/zaudio/COMMIT.
vendor/zaudio/
# Same dirs may exist as symlinks in scratch worktrees pointing at the
# main checkout's vendor/ — keep them out of commits.
vendor/zaudio
vendor/piper1-gpl

# v1.10 menubar Swift Package build dir
ui/menubar/.build/
ui/menubar/build/

playwright-report/
test-results/
.playwright-cache/

# Kokoro model (310M) — fetch via scripts/fetch-kokoro.sh, not committed
assets/kokoro-v1.0.onnx
.venv-kokoro/

# onnxruntime runtime dep (~30MB) — fetch via scripts/fetch-deps.sh
vendor/onnxruntime/

# stray probe test binary + probe wav
test_ort
/tmp/kokoro_probe.wav
