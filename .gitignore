zig-out/
.zig-cache/

node_modules/
dist/
.astro/

.DS_Store
*.log

# v0.6 libpiper FFI: cloned C source + build artifacts (~400MB).
# Reproduce via vendor/README.md. Voice models live in ~/.cache/agent-tts/voices/.
vendor/piper1-gpl/

# v0.7 zaudio + miniaudio vendored sources (~100k LoC single-header lib).
# Reproduce via vendor/README.md. Pinned commit in vendor/zaudio/COMMIT.
vendor/zaudio/
